#!/usr/bin/env rc

if(~ $#winid 0){
	echo "py9lint: Acme is not running."
	exit
}

if(test -f $home/.venv/bin/flake8){
	$home/.venv/bin/flake8 $%
}
if not {
	echo "flake8 not installed."
	exit
}
true
