#!/usr/bin/env rc

if(~ $#winid 0){
	echo "Acme is not running."
	exit
}
yapf=`{which yapf}
tab=$PLAN9/bin/tab

if(~ $#yapf 0) {
	yapf=$tab -n 4 -u
}

echo -n '1,$' | 9p write acme/$winid/addr
9p read acme/$winid/body | $yapf | 9p write acme/$winid/data
exit 0
